name: rna-transcription
version: 1.0.0.3

dependencies:
  - base

library:
  exposed-modules: DNA
  source-dirs: src
  dependencies:
    - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - rna-transcription
      - hspec
